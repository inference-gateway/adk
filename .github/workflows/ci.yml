---
name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@v7.0.0
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v9.3.0
        with:
          version: v2.12.2
          args: --help

      - name: Install Mockgen
        run: go install go.uber.org/mock/mockgen@v0.6.0

      - name: Setup Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 'lts/*'

      - name: Install prettier
        run: npm install -g prettier

      - name: Install markdownlint-cli
        run: npm install -g markdownlint-cli

      - name: Format Go files
        run: find . -name '*.go' -exec gofmt -w {} \;

      - name: Format markdown files
        run: find . -name '*.md' -exec prettier -w {} \;

      - name: Lint markdown examples
        run: markdownlint 'examples/**/*.md'

      - name: Tidy Go modules
        run: find . -name 'go.mod' -execdir go mod tidy \;

      - name: Run generate
        run: |
          go run ./cmd/wrap-a2a-schema schema.yaml schema.openapi.yaml
          go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.8.0 --config=.oapi-codegen.a2a.yaml -o types/generated_types.go schema.openapi.yaml
          gofmt -r 'interface{} -> any' -w types/generated_types.go
          rm -f schema.openapi.yaml
          go run ./cmd/generate-providers

      - name: Format generated markdown
        run: find . -name '*.md' -exec prettier -w {} \;

      - name: Check dirty
        run: git diff --exit-code

      - name: Run Lint
        run: golangci-lint run

      - name: Build
        run: go build .

      - name: Test
        run: go test -v ./...
